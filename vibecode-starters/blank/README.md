# Custom Sandbox

Welcome to your custom VibeCode playground!

## Getting Started
- Edit `index.html`, `style.css`, and `script.js` to build web projects.
- Create new files with any extension (`.py`, `.cpp`, `.java`, `.rs`, `.go`, `.ts`, etc.) to write code in any language.
- Use the **Run** button to execute and preview your creations.
